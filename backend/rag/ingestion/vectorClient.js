// rag/ingestion/vectorClient.js
export async function embedText(text) {
  // call your Python RAG service OR embedding API
}

export async function storeVector(doc) {
  // insert into SAME MongoDB vector collection
}
